# Local development overrides (activate with -Dspring.profiles.active=local or local,dev)
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/easyops
    username: easyops
    password: easyops123

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
